{
  "name": "japavel-framework",
  "version": "0.0.1",
  "private": true,

  "scripts": {
    "dev": "turbo run dev",
    "build:all": "turbo run build",
    "test:all": "turbo run test",
    "lint:all": "turbo run lint",
    "clean": "turbo run clean",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\""
  },
  "devDependencies": {
    "turbo": "^1.12.0",
    "typescript": "^5.3.0",
    "prettier": "^3.2.0",
    "eslint": "^8.56.0",
    "@types/node": "^20.11.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
